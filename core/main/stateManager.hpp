#include "grid.hpp"
#include <string>

class StateManager {
private:
  std::string filePath;

public:
  void save(Grid g, std::string f) {};
  // Grid load(std::string f) {};
  // Grid loadConfig(std::string name) {};
};